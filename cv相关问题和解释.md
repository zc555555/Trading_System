# CV 相关问题和解释 —— 发信前必须能脱稿讲出的七件事

> 发给 Barr 的邮件里每一句话背后的事实。发信之前，必须能不看稿讲出这些事，因为 Barr 大概率会追问。

---

## 第〇步：你的项目本身是什么

你有一个自动化的美股量化系统：**302 只美国大盘股、2014–2026 年的日线数据**。系统里有 6 个"因子"（momentum 动量、trend 趋势、volatility 波动率、alpha、volume 成交量、market 市场），每个因子是一组机器学习模型（XGBoost + LightGBM + CatBoost），吃几十个技术特征，输出对每只股票未来收益的预测。每天收盘后，系统把 302 只股票按预测分数排序，买入排名靠前的，持有一段时间。

**怎么衡量预测得准不准？用 IC（信息系数）。** 做法：某一天，把 302 只股票按"你的预测"排个名次，再按"实际发生的未来收益"排个名次，算这两个名次的相关性（Spearman 相关）。完全猜对 = 1，瞎猜 = 0。每天算一个，长期取平均。真实世界里，顶级量化机构的 IC 也就 0.02–0.05——这行的信号就是这么微弱，所以邮件里那些 0.01 级别的数字不是寒酸，是行业常态。

---

## 第一步：泄漏是怎么回事（邮件第二段的前半）

今年 7 月你做了一次代码审计，在一个叫 **dpo_20**（去趋势价格振荡器）的特征里发现了问题。这个指标的本意是"当前价格减去过去的均线"，衡量价格偏离趋势多远——原料应该全部来自过去。

问题出在一行 pandas 代码。`shift(+11)` 的意思是"取 11 天前的值"（合法），`shift(-11)` 的意思是"取 11 天后的值"（穿越）。代码写反了符号，写成了 `close.shift(-11)`。

**后果**：每天喂给模型的这个特征里，藏着 11 个交易日之后的收盘价。模型不需要学任何真本事——它只要学会"读这个特征"，就等于开了天眼。所以旧回测美得惊人：63.4% 胜率、Sharpe 1.89。**全是假的。**

一个符号，一个字符的 bug。这就是"单个特征里的一个符号错误"。

---

## 第二步："逐位复现"为什么值得写进邮件

修复方式很克制：**只改那一行，其他什么都不动**——同样的数据、同样的训练/验证切分、同样的随机种子，把 18 个模型（6 因子 × 3 种模型）全部重训一遍，跟修复前的结果对比。

结果：

- **momentum 因子**（六个因子里唯一用到 dpo_20 的）：IC 塌方。5 天期从 0.5813 跌到 0.0145，约 **97.5%** 的"预测能力"蒸发（1 天期跌 92%，20 天期跌 90%——"约 90–97%"就是这么来的）。
- **其余四个不含 dpo_20 的因子**：修复前后的 IC **每一位小数都一模一样**。

后一条为什么重要？它是一个**天然对照组**。它证明了两件事：你的流水线是确定性的（没有随机噪声干扰对比），以及塌方**只能**归因于那一个被修的特征——排除了"是不是重训本身导致数字变了"的所有其他解释。这相当于做实验时的控制变量法。"把塌方精确地钉在泄漏上"说的就是这个。

---

## 第三步：重建评估——purged walk-forward 是什么

泄漏修完后，你面临的问题是：旧的评估方法本身就不可信，得推倒重来。新框架叫 **purged walk-forward**（带净化的滚动前向验证）：

- **walk-forward（滚动前向）**：模拟真实生活。用 2019–2021 三年数据训练 → 在紧接着的一个季度上测试 → 窗口往前滚一个季度 → 重新训练 → 再测下一个季度……如此滚 **17 次**。任何时刻，模型都只见过"当时的过去"，测试永远发生在"当时的未来"。17 个测试季度拼起来 = **1,071 个样本外交易日**——这就是邮件里那个数字。
- **purged（净化）**：一个精细的坑。假设你预测"未来 5 天收益"，那么训练集最后一天的标签（那 5 天的收益）会伸进测试期里——训练数据变相偷看了测试期。解决办法：把每个训练窗口末尾的"预测期长度 + 5 天缓冲"直接删掉。这就是 **purge + embargo**。

在这个诚实的框架下重测，结论：**1 天期混合 IC = +0.0025，t = 0.57**。

**t 值是什么**：粗糙地说，t 是"平均信号 ÷ 噪声"的比值，回答"这个平均值是真本事还是运气"。经验法则：t > 2 ≈ 有 95% 以上把握不是运气；t = 0.57 ≈ **和零没区别**。所以诚实的结论是：修完泄漏后，你的系统在 1 天周期上没有任何预测能力——恰好就是那篇文章汇率实验的结论（"没人打得过 naive"）。"幸存下来的结果恰好落在她汇率实验的位置"，说的就是这个对应。

---

## 第四步：5 天期的信号 + 两个统计上的诚实动作（邮件第二段后半）

IC 归零之后你没有放弃，而是换了个问题：**也许 1 天太短，信号在更长的周期上？** 于是把预测目标从"未来 1 天收益"换成 5 天、20 天，把整个 walk-forward 重跑。

结果：**5 天期上第一次出现了真信号**——trend 因子 IC +0.0108（t=2.27）、volatility 因子 +0.0177（t=2.06），双双过了 t=2 线。这是整个项目第一次拿到"统计上站得住"的诚实结果。

但你（的评估代码）做了两个自我约束的动作，这两个动作正是邮件里最加分的部分：

1. **Newey-West 校正**：如果你每天都预测"未来 5 天收益"，那么周一的标签（周一~周五）和周二的标签（周二~周六）共享了 4 天——相邻两天的 IC 天然高度相关，不是独立样本。假装它们独立，t 值会虚高约 √5 ≈ 2.2 倍——很多平庸信号就是这样被"显著"出来的。Newey-West 是计量经济学的标准修正方法，把这个虚高压回去。**你的 t=2.27 是修正后的数字。**（注意这正好又是那篇文章"有效样本量 < 行数"的主题——你的代码里早就在处理这个问题了。）
2. **多重检验的自我坦白**：这轮实验你一共试了大约 **18 个"因子 × 周期"组合**。试的次数多了，纯靠运气也会蹦出几个 t>2（就像 18 个人各抛 20 次硬币，总有人连出 15 个正面）。所以 commit 记录里明确写着：18 个假设、2 个过线——**"strong lead, not proof"（强线索，非证明）**。这就是"所以我对它持保留态度"的出处。

"信息无法凭空制造，但可以让误差棒不再撒谎"总结的就是这一整步：NW 校正和 purge 都没有创造信号，它们只是让你对自己手里到底有多少信号不再自欺。

---

## 第五步：隔离 holdout 和 vol_ext 的故事（邮件第三段第一个案例）

**隔离 holdout**：2025 年 7 月 1 日之后的所有数据被"封存"——所有调参、选择、决策都不许碰它，它只用于最终验收。逻辑和高考卷子密封一样：你每偷看一次并据此调整，它就不再是"没见过的未来"了。

然后来了考验。你试验一个候选的第 7 因子 **vol_ext**（一组更精细的波动率特征），它在封存的 holdout 段上跑出 **t = 2.50**——按刚才的经验法则，"显著"了，看起来该采纳。

但这里有个疑点：holdout 只有 10 个月。一个因子完全可能只是碰巧适合这 10 个月的市场风格（**regime luck，行情运气**）。怎么检验？正常做法是等——再攒一年新数据。你们用了个聪明的替代：把数据集的起点从 2018 回补到 2014，凭空多出约 4 年历史。关键在于：这 4 年（**1,007 个测试日**）从未被任何一次实验碰过——对所有模型来说它是"从未见过的考题"，作用等价于未来的新数据。这就是**"处女数据"（virgin data）**。

而且在算出结果之前，**采纳规则先写进了分析脚本并提交了 git**——三个条件白纸黑字：(1) 候选因子在处女期不能系统性为负；(2) 加入它不能拖累整体组合在处女期的表现；(3) 最新一段数据上方向有利。这叫**预注册（pre-registration）**——先立规则再看结果，堵死"看到结果后挪门柱"的人性漏洞（git 的提交时间戳就是公证人）。

**裁决**：vol_ext 在那 1,007 个处女测试日上 IC 为负（-0.0077，t=-0.94——注意不显著为负，所以邮件里诚实地写了"虽不显著"）。四个数据段里它只在 holdout 那一段是正的——这正是"行情运气"的典型指纹。**拒收。** 一个 t=2.5 的诱惑，被一个下午的处女数据检验证伪了。

---

## 第六步：GDELT 新闻因子（邮件第三段第二个案例）

这就是你的"外生信息"实验——恰好是那篇文章结论让大家去做的事。

**GDELT** 是一个公开的全球新闻数据库。你通过 Google BigQuery 写 SQL，把新闻里对各家公司的提及匹配到你的 302 只股票（匹配上 262 只），聚合成每只股票每天的新闻情绪分和报道量，从中构造了 **5 个特征**（当日情绪、5 日情绪变化、60 日情绪 z 分数、报道量激增、情绪稳定度）——全部只用截至当天的新闻，时间对齐上特意做了保守处理，确保零穿越。

然后让它过和 vol_ext 同一道预注册闸门。结果：**5 天期上，三条条件全部通过**——处女期为正、不拖累组合、新数据段有利。它是史上第一个（也是唯一一个）通过这道闸门的候选因子。**外生信息真的带来了信号。**

但故事还有下半段：你的系统实际的持仓周期是 20 天（原因见下一步）。在 20 天期上重测：新闻因子的 IC 是 **-0.0004，t=-0.10——精确的零**。解释很符合直觉：新闻的信息价值衰减极快，5 天内还有效，拖到 20 天就被市场完全消化了。你等于直接测量了**新闻信息的半衰期**。

所以最终决定：**不进生产。** 哪怕它通过了全部预注册条件——因为它在系统真正交易的周期上没有价值。"找到数据用了一个周末；决定能不能相信它，用掉了整个评估框架"——前半句是事实（BigQuery 拉数据确实很快），后半句也是事实（裁决它用掉了 walk-forward、处女分段、预注册这整套机器）。

---

## 第七步：为什么是 20 天持仓（背景知识，防追问）

每笔交易有成本（佣金 + 滑点，你的模型按一来一回约 0.3% 计）。持有 1 天，这 0.3% 每天都在割你——模拟结果是**年化 -53%**，惨烈亏损。持有 20 天，同样的成本摊到 20 天里，模拟结果约打平（Sharpe +0.04）。所以生产系统选了 20 天持仓——不是因为 20 天信号最强（5 天才最强），而是因为**只有 20 天扛得起交易成本**。

如果 Barr 问 "so does it make money?"，诚实答案是：**扣除成本后约打平，目前是模拟/纸面运行**——对这位教授，诚实的零比吹嘘的正数加分得多。

---

## 对照表：邮件里的话 ↔ 对应的事实

| 邮件里的话 | 对应的事实 |
|---|---|
| "单个特征里的符号错误 shift(-11)" | 第一步：dpo_20 的 pandas bug |
| "约 97% 表观 IC 消失" | 第二步：5 天期 0.5813→0.0145 |
| "不含该特征的因子 IC 逐位复现" | 第二步：天然对照组 |
| "1,071 个样本外交易日、IC +0.0025、t=0.57" | 第三步：17 折 purged walk-forward 的诚实基线 |
| "trend t=2.27、volatility t=2.06" | 第四步：5 天期横向实验 |
| "18 个假设仅 2 个过线"、"Newey-West、√h" | 第四步：两个自我约束动作 |
| "t=2.50 却在 1,007 个处女测试日为负" | 第五步：vol_ext 被预注册规则拒收 |
| "GDELT 通过三条件、20 天期归零" | 第六步：新闻因子的一生 |
| "我的系统实际持有的 20 天期" | 第七步：成本决定持仓周期 |

---

## 发信前给自己的最低要求

不看稿能讲出——

1. 泄漏是什么
2. 为什么"其他因子逐位复现"证明了归因
3. IC 和 t>2 各自是什么意思
4. purge 在防什么
5. 为什么 18 选 2 要打折扣
6. vol_ext 为什么被拒
7. 新闻因子为什么通过了还不用

能讲出这七件事，Barr 怎么追问都接得住——因为这些确实是仓库里真实发生过的事，git 历史和结果文件都在。
