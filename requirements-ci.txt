# Lean dependency set for the test suite (CI). The modules under test import
# only numpy/pandas at module level; ML/NLP/broker libraries are lazily
# imported inside training/trading functions the tests never call.
# Production installs use requirements.txt.
pandas>=2.2.0,<3.0
numpy>=1.26.0,<3.0
scipy>=1.12.0,<2.0
pyarrow>=15.0.0,<20.0
pyyaml>=6.0.1,<7.0
pytz>=2024.1
pytest>=8.0
