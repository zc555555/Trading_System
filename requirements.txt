# =============================================================================
# Runtime dependencies for stock_predict
# =============================================================================
# Pinned with conservative upper bounds to keep production reproducible.
# Update with care; re-run smoke tests after any version bump.
# Install:  pip install -r requirements.txt
# =============================================================================

# --- Core data / numerics ----------------------------------------------------
pandas>=2.2.0,<3.0
numpy>=1.26.0,<3.0
scipy>=1.12.0,<2.0
pyarrow>=15.0.0,<20.0          # parquet I/O

# --- Market data / broker ----------------------------------------------------
yfinance>=0.2.36,<0.3
alpaca-py>=0.30.0,<1.0
finnhub-python>=2.4.20,<3.0

# --- Machine learning --------------------------------------------------------
scikit-learn>=1.4.0,<2.0
catboost>=1.2.5,<2.0
lightgbm>=4.3.0,<5.0
xgboost>=2.0.3,<3.0

# --- NLP / sentiment (FinBERT) ----------------------------------------------
transformers>=4.40.0,<5.0
torch>=2.2.0,<3.0

# --- Visualization / dashboard ----------------------------------------------
streamlit>=1.32.0,<2.0
plotly>=5.20.0,<6.0
matplotlib>=3.8.0,<4.0
seaborn>=0.13.0,<1.0

# --- Scheduling / utilities --------------------------------------------------
schedule>=1.2.0,<2.0
pytz>=2024.1
requests>=2.31.0,<3.0
pyyaml>=6.0.1,<7.0
tqdm>=4.66.0,<5.0

# Note: zoneinfo is in stdlib for Python >= 3.9; no backport needed.
