P0 — 立即修复:让所有结论失效的正确性 bug
1. dpo_20 特征是未来函数(最严重)。 operators.py:770 计算 DPO 时用了 shift(-11),即把 11 个交易日之后的收盘价放进了特征:dpo_20[t] = close[t+11] − SMA20[t](正确的 DPO 应该用正向 shift 取过去的价格)。这个特征在权重最高的 momentum 因子里(factor_definitions.py:56),也在硬编码的 60 个入选特征里(update_selected_features.py:67)。这完美解释了为什么 IC 高得离谱(业界顶级日频 rank IC 约 0.02–0.08,你们报了 0.26–0.65)。修复后必须全量重建特征、重训、重新评估——预期 IC 会大幅下降,那才是真实水平。

2. legacy 交易路径把 SELL 信号当 BUY 执行。 alpaca_trader.py:265 对信号列表里的每只股票一律 'buy',不看预测方向。而信号里约一半是负预测(如 5 月 18 日信号第 1 名 MU 预测 −0.758 是做空信号)。也就是说 legacy 路径会满仓买入模型最想做空的股票。另外 max_position_pct=0.15 在 alpaca_trader.py:58 定义后从未被引用——单票仓位实际上没有上限。

3. 回测引擎从未使用过模型预测。 run_backtest_multi_factor.py:174-185 里 zip(models, weights) 对两个 dict 做 zip,迭代出来的是键字符串,'xgb'.predict(X) 抛异常后被裸 except: continue 吞掉,所有预测恒为 0,回测实际交易的是按索引顺序取的固定前 10 只股票。任何历史回测曲线都无意义。

4. 双系统冲突:配置说 staggered,自动化跑 legacy。 config_trading.py:16 声明 STRATEGY = "staggered"(W2-B 五日错峰持仓),但所有启动器(自动交易.bat → scripts/run_trading.bat → run_auto_trading.py)仍指向 legacy 单日系统;run_staggered_trading.py 没有任何调度包装,tranche 注册表文件从未生成过(只 dry-run 过)。更危险的是 run_auto_trading.py:97-111 会无条件清掉所有持仓——如果两套系统先后运行,legacy 会摧毁 staggered 的多日仓位并让注册表失去同步。必须二选一并物理删除/禁用另一条路径。

5. staggered 路径的仓位追踪不可靠。 run_staggered_trading.py:103-106 不管平仓订单是否成功都 mark_closed(失败的仓位从此无人管理);开仓订单在循环内逐笔提交但注册表在循环结束后才保存(:229 vs :323)——中途任何异常都会留下broker 有持仓、注册表无记录、没有止损的裸仓位。下单确认也只轮询 5 秒就默认成功(alpaca_trader.py:205-206)。

P1 — 重建可信的评估体系("先修尺子,再量东西")
6. 用 purged walk-forward 替代单次 80/20 切分。 现在训练用单次时间切分且无 purge/embargo(train_multi_factor_models.py:183-188),训练末尾样本的标签直接跨入验证期。讽刺的是项目里已经有一个带 purge、交易成本、Sortino/Calmar 的正经引擎 walk_forward.py——但它是死代码,从未接入多因子管线,而且 embargo_days 声明了却没实现(:247-249)。把它修好、接上,作为唯一的评估入口,embargo 至少设为最长标签周期(20 天)。

7. IC 换成按日截面 rank IC。 现在的"IC"是全验证集 pooled Pearson 相关(train_multi_factor_models.py:120),混合了时序与截面信号;对 20 天重叠标签(相邻样本共享 19/20 的标签窗口),自相关会把数字吹到毫无意义。正确做法:每个交易日算一次横截面 Spearman 相关,再对日均值取平均并报 t 统计量。这一条不修,任何因子比较、周期选择、权重分配都建立在噪声上。

8. 消除选择偏差,建立不可触碰的 hold-out。 W2-A 在同一个验证集上既选最优周期又把该周期的 IC 写进生产权重(promote_best_horizon_models.py:84-98),等于"考完试挑最高分再把这个分数当预期成绩"。用户看到的 63.4% 胜率同样来自模型训练/验证过的最近 500 行(get_daily_signals_multi_factor.py:57-60)——纯样本内。方案:三段式切分(训练 / 选择用验证 / 从不参与任何决策的最终测试段),或在 walk-forward 每折内做嵌套选择。

9. 回测对齐真实执行。 修好 zip bug 之后还要:加成本(walk_forward.py 里现成的 5bps 佣金 + 10bps 滑点)、信号当日收盘计算→次日执行(现在是同日收盘成交的轻微前视)、仓位逻辑与生产一致(现在回测是递减的 cash 比例 + 只做多,生产是 confidence 加权 + 双向)。然后用修复后的系统在 302 股票宇宙上跑一次诚实的基线回测——这是后续所有优化的对照组。

10. 数据层的次级泄漏。 顺手修:warm-up 期 bfill 引入未来值(build_dataset.py:717,改为留 NaN 并丢弃);流动性过滤用"今天的"成交额决定整段历史去留 + 宇宙用当前成分股 = 幸存者偏差(短期可先文档化为已知限制,长期做 point-in-time 宇宙);market breadth 恒等于 0 的死 bug(build_dataset.py:540-548)。

P2 — 交易执行与风控加固
11. 止损上 broker 端。 现在全部订单是裸市价单,止损/止盈完全靠 5 分钟轮询的监控进程合成——监控只在盘中跑,多日持仓的隔夜跳空完全无保护,而隔夜风险恰恰是 staggered 策略的主要风险。Alpaca 支持 bracket/OCO 订单(StopLossRequest 已经 import 了却没用,alpaca_trader.py:22-25),开仓时直接带上 ATR 止损价即可,监控进程降级为对账与告警。

12. 执行细节。 统一价格源(现在 yfinance 报价定仓位、Alpaca 市价成交、平仓盈亏又用 yfinance 估算);加开市检查(代码里没有任何 get_clock,闭市下单会在次日开盘以未知价格成交);下单前检查 buying power;交易日历考虑美股假日(trading_calendar.py 目前只排除周末)。

13. 风控闭环。 账户级 3% 日内 kill switch 依赖监控进程活着才生效——加一个独立的守护检查(哪怕是定时任务里的最简 5 行脚本);staggered 的注册表要与 broker 实际持仓定期对账(监控强平后注册表仍显示持仓的问题在 W2-C 文档里自己也承认了)。

P3 — 工程卫生
14. 测试从 0 到 1。 pytest 配置齐全但 tests/ 目录不存在。最优先补两类:防泄漏测试(全表扫描特征代码禁止 shift(-、标签对齐断言、"特征日期 ≤ 信号日期"的合成数据测试——dpo_20 这种 bug 一个测试就能拦住)和核心指标的 golden 值测试。其次是 tranche registry 的状态机测试。

15. 清理死配置与三份真相。 config.yaml 里的模型超参(500 棵树、depth 6)是死配置——训练脚本硬编码了 100 棵树/depth 5(train_multi_factor_models.py:55-97);label.type: classification 也被忽略(实际是回归)。要么让训练读 config,要么删掉误导性配置。同类问题:每日管线存在三份不一致的实现(run_auto_trading / run_menu / 文档 cron,menu 还少跑流动性过滤这一步)、prepare_prediction_data_with_latest.py 是过时副本、菜单里多个入口指向不存在的脚本(run_menu.py:112)、多处横幅文案还写着已废弃的 ±2.5% 止损。

16. 管线幂等化与 retrain 补链。 prepare_prediction_data.py 把特征原地写回 stocks.parquet(:96),重复运行会在已加工数据上再加工(目录里的 .bak 文件就是这个问题的疤痕)——改为 raw 与 features 分文件。更重要的:run_retrain_models.py 抓完新数据后不重建特征就直接重训,模型学的是上一次构建的旧特征——把 prepare_prediction_data.py 插进重训流程。

17. git 与安全。 整个仓库只有 1 个提交、50 个未提交变更——先把当前状态提交了,之后小步提交。密钥文件确实没进 git(我验证过),但项目在 OneDrive 同步目录里,明文 API key 会同步上云,venv(约几 GB)也在被同步——建议项目移出 OneDrive 或至少把密钥改为环境变量。

P4 — 模型与策略升级(尺子修好之后才做)
18. 信号周期与持仓周期对齐。 现在 momentum/trend/volatility 预测 20 天、market/volume 预测 5 天、alpha 预测 1 天,z-score 后混成一个信号,却用 1–5 天的持仓 + ATR 止损去交易。要么统一预测 5 天(与 staggered 持仓期一致),要么按周期分账本各自交易。混合信号的每日 z-score 已经解决了量级问题,但语义(20 天的观点用 1 天的仓位表达)没解决。

19. 因子层瘦身。 volume 因子历经 W1.6 重构后 IC 仍约 0(0.004),market 因子的特征在截面上恒同值(自家文档承认"模型无法从相同特征提取信号")——继续每周重训它们是纯浪费。砍掉或降级为 regime 条件变量(现成的 market_regime.py 完全没接线,可以让它来消化 market 信息:VIX 高位降杠杆、按牛熊切换因子权重)。

20. 用起来已经抓好的另类数据。 research/data/ 里已经躺着 news_features.parquet、iv_features.parquet、option_iv.parquet、GDELT 数据——抓取脚本都写完了,但没有一个接入了模型特征。在启动 16 周计划里 Phase B 的付费 Polygon 订阅之前,先把这些免费存量数据喂给模型验证有没有增量 IC,成本为零。

21. 对 16 周计划的修正建议。 原计划(GARCH → IV → 截面排名 → Transformer → 组合优化)方向都对,但它假设当前基线可信——实际不可信。建议在 Phase B 前插入一个 "Phase A′:评估体系重建"(即上面 P0+P1,约 1–2 周),否则后面每个实验的"提升"都无法与真实基线比较,16 周会白跑。另外 Phase C 的 LambdaRank 截面排名与 W7 计划一致,值得保留;Transformer(W9)建议推到有了可信评估之后再说。