"""Data-vendor API keys -- TEMPLATE.

Copy to ``config_keys.py`` (git-ignored) and fill in, or export the same
names as environment variables (environment wins). Never paste keys into
chat, commits, or logs.

  SHARADAR_API_KEY : sharadar.com -> Subscribe -> Prices ($9/mo personal)
                     "test-api-key" works for the free DJIA-30 sample.
  MASSIVE_API_KEY  : massive.com (ex-Polygon) -> free Basic tier is enough
                     for the options IV prototype (5 calls/min, 2y history).
"""

SHARADAR_API_KEY = ""
MASSIVE_API_KEY = ""
