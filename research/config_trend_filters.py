"""
长期趋势过滤器配置

用于过滤掉长期下跌趋势的股票，避免"逆势抄底"
"""

# ============================================================================
# 长期趋势过滤阈值
# ============================================================================

# 7天收益率下限（建议：-2% 到 -5%）
# - 更严格（如-2%）：只选择近期表现好的股票，可能错过反弹
# - 更宽松（如-5%）：允许一些短期回调，但可能选到下跌股
FILTER_7D_MIN_RETURN = -3.0  # %

# 14天收益率下限（建议：-5% 到 -10%）
# - 用于识别中期趋势
# - 如果14天跌幅>6%，说明中期趋势较弱
FILTER_14D_MIN_RETURN = -6.0  # %

# 30天收益率下限（可选，默认不启用）
# - 设置为None则不过滤
# - 设置为-10.0则过滤掉30天跌幅>10%的股票
FILTER_30D_MIN_RETURN = None  # % or None

# 趋势强度下限（MA7 vs MA30）
# - 正值：要求短期均线 > 长期均线（上升趋势）
# - 负值：允许一定的下跌趋势
# - 建议：-3% 到 +2%
FILTER_TREND_STRENGTH_MIN = -3.0  # %

# 价格相对MA位置过滤（可选）
# - True: 要求当前价格 > MA7 > MA14（强势上升）
# - False: 不要求（推荐，避免过滤掉正在企稳的股票）
REQUIRE_PRICE_ABOVE_MAs = False

# ============================================================================
# 推荐配置场景
# ============================================================================

# 保守型（避免所有下跌趋势）:
# FILTER_7D_MIN_RETURN = -1.0
# FILTER_14D_MIN_RETURN = -3.0
# FILTER_TREND_STRENGTH_MIN = 0.0
# REQUIRE_PRICE_ABOVE_MAs = True

# 标准型（当前设置，平衡风险和机会）:
# FILTER_7D_MIN_RETURN = -3.0
# FILTER_14D_MIN_RETURN = -6.0
# FILTER_TREND_STRENGTH_MIN = -3.0
# REQUIRE_PRICE_ABOVE_MAs = False

# 激进型（允许反弹交易）:
# FILTER_7D_MIN_RETURN = -5.0
# FILTER_14D_MIN_RETURN = -10.0
# FILTER_TREND_STRENGTH_MIN = -5.0
# REQUIRE_PRICE_ABOVE_MAs = False

# ============================================================================
# 使用说明
# ============================================================================

"""
如何调整：

1. 如果发现选出的股票还是在长期下跌：
   - 降低 FILTER_7D_MIN_RETURN（如从-3改成-2）
   - 降低 FILTER_14D_MIN_RETURN（如从-6改成-4）
   - 提高 FILTER_TREND_STRENGTH_MIN（如从-3改成0）

2. 如果发现过滤太严格，选不出股票：
   - 提高 FILTER_7D_MIN_RETURN（如从-3改成-5）
   - 提高 FILTER_14D_MIN_RETURN（如从-6改成-10）
   - 降低 FILTER_TREND_STRENGTH_MIN（如从-3改成-5）

3. 如果想完全避免下跌股票：
   - 使用"保守型"配置
   - 设置 REQUIRE_PRICE_ABOVE_MAs = True

4. 测试配置：
   运行: test_improved_strategy.bat
   查看有多少股票被过滤，是否合理
"""
